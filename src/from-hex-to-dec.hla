program ConvertFromHexToDecimal;
#include("stdlib.hhf");

static
    value: int32;

begin ConvertFromHexToDecimal;
    stdout.put("Hex number -> ");
    stdin.get(eax);
    mov(eax, value);
    stdout.put("Decimal number -> ", value, nl);
end ConvertFromHexToDecimal;