program HexIO;
#include("stdlib.hhf")

static
    i32: int32;

begin HexIO;
    stdout.put("Hex number -> ");
    stdin.geth32();
    mov(eax, i32);
    stdout.put("$");
    stdout.puth32(i32);
    stdout.newln();
end HexIO;
